<?php

 if ($_SERVER['SERVER_NAME'] == 'localhost')
        $conn = new mysqli("p:" . "localhost", "root", "", "livesettings");
    else
        $conn = new mysqli("p:" . "localhost", "uaemast_arieseso", "fV_ywT)iZeTo", "uaemast_mastech");


// Create connection
// Check connection
    if ($conn->connect_error) {
        die("Connection failed: " . $conn->connect_error);
    }
